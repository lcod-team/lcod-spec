{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "projectPath": {
      "type": "string",
      "minLength": 1,
      "description": "Base directory of the project invoking the resolver."
    },
    "cacheDir": {
      "type": "string",
      "minLength": 1,
      "description": "Directory used to cache downloaded catalogues."
    },
    "sourcesPath": {
      "type": "string",
      "minLength": 1,
      "description": "Explicit path to a sources.json file. Defaults to <projectPath>/sources.json."
    },
    "resolverConfig": {
      "type": "object",
      "description": "Resolved resolver configuration (resolve.config.json) used for fallbacks."
    }
  },
  "required": [
    "projectPath"
  ],
  "description": "Resolve resolver sources.json into inline registry catalogue entries."
}