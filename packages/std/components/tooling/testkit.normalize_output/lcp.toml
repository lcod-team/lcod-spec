schemaVersion = "2.0"
id = "lcod://tooling/testkit/normalize_output@0.1.0"
version = "0.1.0"
kind = "component"
summary = "Normalize slot outputs for testkit reports."

[palette]
category = "Testing"
icon = "mdi:beaker"
tags = ["test", "report"]

[deps]
requires = ["lcod://tooling/script@1"]

[inputs.value]
summary = "Raw value returned by a test slot."
required = false
schema = """{}"""

[outputs.value]
summary = "Normalized structure (value/warnings/error) when available."
schema = """{}"""
