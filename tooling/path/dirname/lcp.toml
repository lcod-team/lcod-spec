schemaVersion = "1.0"
id = "lcod://tooling/path/dirname@0.1.0"
name = "dirname"
namespace = "tooling/path"
version = "0.1.0"
kind = "component"

summary = "Return the parent directory of a given path."

[palette]
category = "Filesystem"
icon = "mdi:folder-arrow-left"
tags = ["path", "dirname"]

[docs]
readme = "README.md"

[[io.input]]
name = "path"
type = "string"
required = true
description = "Target path."

[[io.output]]
name = "dirname"
type = "string"
description = "Parent directory."

[deps]
requires = [
  "lcod://tooling/script@1"
]
