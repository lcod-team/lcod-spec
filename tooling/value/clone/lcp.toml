schemaVersion = "2.0"
id = "lcod://tooling/value/clone@0.1.0"
version = "0.1.0"
kind = "component"
summary = "Deep clone a JSON value."

[palette]
category = "Utility"
icon = "mdi:content-copy"
tags = ["value", "clone"]

[deps]
requires = [
  "lcod://contract/core/value/clone@1"
]

[inputs.value]
summary = "Value to clone."
required = false
schema = "{}"

[outputs.value]
summary = "Independent copy of the provided value."
schema = "{}"
