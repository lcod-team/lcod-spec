schemaVersion = "2.0"
id = "lcod://tooling/sanitizer/probe@0.1.0"
version = "0.1.0"
kind = "component"
summary = "Observes sanitised inputs to ensure declared payloads remain reachable."

[palette]
category = "Tooling"
icon = "mdi:shield-search"
tags = ["sanitizer", "debug"]

[deps]
requires = [
  "lcod://axiom/state/raw_input@1",
  "lcod://tooling/value/default_object@0.1.0",
  "lcod://impl/set@1"
]

[inputs.expect]
summary = "Payload expected to be echoed by the sanitiser"
required = false
schema = """{}"""

[outputs.result]
summary = "Probe report"
schema = """{"type":"object"}"""
