{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "tooling/compose/normalize@1 output",
  "type": "object",
  "required": ["compose"],
  "additionalProperties": false,
  "properties": {
    "compose": {
      "description": "Normalized array of compose steps (explicit mappings).",
      "type": "array",
      "items": { "type": "object" }
    }
  }
}
