schemaVersion = "2.0"
id = "lcod://contract/core/number/trunc@1.0.0"
version = "1.0.0"
kind = "contract"
summary = "Truncate a finite number toward zero."

[inputs.value]
summary = "Number to truncate."
required = true
schema = """
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "number"
}
"""

[outputs.value]
summary = "Truncated integer."
schema = """
{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "type": "number"
}
"""
