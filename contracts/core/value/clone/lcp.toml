schemaVersion = "2.0"
id = "lcod://contract/core/value/clone@1.0.0"
version = "1.0.0"
kind = "contract"
summary = "Deep clone a JSON value."

[tool]
name = "core_value_clone"
description = "Deep clone a JSON value."
inputSchema = "schema/clone.in.json"
outputSchema = "schema/clone.out.json"

[documentation]
body = """
Deep clone a JSON value.
"""
