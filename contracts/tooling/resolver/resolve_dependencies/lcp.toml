schemaVersion = "2.0"
id = "lcod://contract/tooling/resolver/resolve_dependencies@1.0.0"
version = "1.0.0"
kind = "contract"
summary = "Resolve dependency graph for lcod-resolver."

[tool]
name = "tooling_resolver_resolve_dependencies"
description = "Resolve dependency graph for lcod-resolver."
inputSchema = "schema/resolve_dependencies.in.json"
outputSchema = "schema/resolve_dependencies.out.json"

[documentation]
body = """
Resolve dependency graph for lcod-resolver.
"""
