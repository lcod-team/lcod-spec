schemaVersion = "2.0"
id = "lcod://tests/spec/tooling_input_sanitizer@0.1.0"
version = "0.1.0"
kind = "component"
summary = "Ensures component input sanitiser preserves declared expect payloads."

[palette]
category = "Spec"
icon = "mdi:shield-check"
tags = ["sanitizer", "test", "spec"]

[deps]
requires = [
  "lcod://tooling/test_checker@1",
  "lcod://tooling/sanitizer/probe@0.1.0",
  "lcod://impl/set@1"
]

[outputs.report]
summary = "Result forwarded from tooling/test_checker@1"
