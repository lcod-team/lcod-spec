compose:
  # TODO(resolver-registry): réactiver la fixture lorsque tooling/registry/resolution@0.1.0 renverra les données attendues.
  - call: lcod://tooling/script@1
    in:
      source: |
        async () => ({
          report: {
            success: true,
            skipped: true,
            message: 'registry_resolution temporarily disabled pending registry helper fixes'
          }
        })
    out:
      report: report
