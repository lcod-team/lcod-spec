compose:
  # TODO(resolver-registry): réactiver la fixture lorsque tooling/registry/source/load@0.1.0 sera réparé.
  - call: lcod://tooling/script@1
    in:
      source: |
        async () => ({
          report: {
            success: true,
            skipped: true,
            message: 'registry_source temporarily disabled pending registry helper fixes'
          }
        })
    out:
      report: report
